% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-normalised-prescribing.R
\name{get_normalised_prescribing}
\alias{get_normalised_prescribing}
\title{Get normalised prescribing}
\usage{
get_normalised_prescribing(
  con,
  bnf_codes = NULL,
  start_date = NULL,
  end_date = NULL
)
}
\arguments{
\item{con}{A BigQuery connection from \code{\link[=connect_bq]{connect_bq()}}.}

\item{bnf_codes}{Character vector of BNF codes to keep, e.g.
\code{"0601022B0AAASAS"}. \verb{\%} anywhere in a code is a wildcard matching any
characters, so \code{"0404000M0\%"} keeps all methylphenidate presentations.
Default \code{NULL} (no filter).}

\item{start_date}{Earliest prescribing month to keep, as a \code{Date} or
\code{"YYYY-MM-DD"} string. Default \code{NULL} (no filter).}

\item{end_date}{Latest prescribing month to keep, as a \code{Date} or
\code{"YYYY-MM-DD"} string. Default \code{NULL} (no filter).}
}
\value{
A query (\code{tbl}) of the \code{normalised_prescribing} table, to be
downloaded with \code{\link[dplyr:collect]{dplyr::collect()}}.
}
\description{
Queries the \code{normalised_prescribing} table, with optional filtering by
BNF codes and prescribing month. This is the table you'll usually want,
rather than the raw prescribing table.
}
\details{
The result is a query, not data: nothing is downloaded until you call
\code{\link[dplyr:collect]{dplyr::collect()}}, and you can add further \code{dplyr} steps first. The full
table is very large, so filter by BNF code and date range before
collecting.
}
\examples{
\dontrun{
con <- connect_bq()

# Prescribing of two presentations between January and March 2023
get_normalised_prescribing(
  con,
  bnf_codes = c("0408010W0BBAKA1", "0601022B0AAASAS"),
  start_date = "2023-01-01",
  end_date = "2023-03-01"
) |>
  dplyr::collect()

# Prescribing of all methylphenidate presentations, using a wildcard
get_normalised_prescribing(
  con,
  bnf_codes = "0404000M0\%",
  start_date = "2023-01-01",
  end_date = "2023-03-01"
) |>
  dplyr::collect()
}
}
