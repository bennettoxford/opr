% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-practices.R
\name{get_practices}
\alias{get_practices}
\title{Get practices}
\usage{
get_practices(
  con,
  add_setting_labels = FALSE,
  filter_setting = NULL,
  add_status_code_labels = FALSE,
  filter_status_code = NULL
)
}
\arguments{
\item{con}{A BigQuery connection from \code{\link[=connect_bq]{connect_bq()}}.}

\item{add_setting_labels}{Logical, add a \code{setting_label} column describing
the practice setting code. Default \code{FALSE}.}

\item{filter_setting}{Integer vector of setting codes to keep, e.g. \code{4} for
standard GP practices. Default \code{NULL} (no filter). See \link{practice_settings}.}

\item{add_status_code_labels}{Logical, add a \code{status_code_label} column
describing the practice status code. Default \code{FALSE}.}

\item{filter_status_code}{Character vector of status codes to keep, e.g.
\code{"A"} for active practices. Default \code{NULL} (no filter). See \link{practice_statuses}.}
}
\value{
A query (\code{tbl}) of the \code{practices} table, to be downloaded with
\code{\link[dplyr:collect]{dplyr::collect()}}.
}
\description{
Queries the \code{practices} table, with options to add descriptive labels for
the \code{setting} and \code{status_code} columns and to filter by those columns.
Labels are defined in the \link{practice_settings} and \link{practice_statuses}
lookup tables.
}
\details{
The result is a query, not data: nothing is downloaded until you call
\code{\link[dplyr:collect]{dplyr::collect()}}, and you can add further \code{dplyr} steps first.
}
\examples{
\dontrun{
con <- connect_bq()

# All practices, unfiltered
get_practices(con)

# Standard GP practices with labels, downloaded into R
gp_practices <- get_practices(
  con,
  add_setting_labels = TRUE,
  filter_setting = 4
) |>
  dplyr::collect()
}
}
