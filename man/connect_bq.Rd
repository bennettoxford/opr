% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect-bq.R
\name{connect_bq}
\alias{connect_bq}
\title{Connect to the OpenPrescribing BigQuery database}
\usage{
connect_bq(
  project = "ebmdatalab",
  dataset = "hscic",
  billing = "ebmdatalab",
  credentials_var = "OP_CREDENTIALS",
  quiet = FALSE
)
}
\arguments{
\item{project}{Character, the BigQuery project. Default \code{"ebmdatalab"}.}

\item{dataset}{Character, the BigQuery dataset to connect to. Default \code{"hscic"}.}

\item{billing}{Character, the BigQuery project to bill. Default \code{"ebmdatalab"}.}

\item{credentials_var}{Character, the name of the environment variable that
holds the path to your BigQuery credentials JSON file. Default \code{"OP_CREDENTIALS"}.}

\item{quiet}{Logical, suppress informational messages. Default \code{FALSE}.}
}
\value{
A \code{DBIConnection} to BigQuery that can be used with \code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}},
\code{\link[dplyr:tbl]{dplyr::tbl()}}, and the \verb{get_*()} functions in this package.
}
\description{
Authenticates with BigQuery using a service account credentials file and
returns a database connection. The path to the credentials file is read
from an environment variable (by default \code{OP_CREDENTIALS}), so credentials
never need to be hardcoded in analysis scripts.
}
\details{
See \code{vignette("connect-bigquery", package = "opr")} for how to get
credentials and set up the environment variable.
}
\examples{
\dontrun{
con <- connect_bq()

# List all available tables
DBI::dbListTables(con)

# Disconnect when done
DBI::dbDisconnect(con)
}
}
